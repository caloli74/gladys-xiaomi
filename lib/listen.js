/* daemon to listen messages sent by the gateway */
var shared = require('./shared.js');
var Xiaomi = require('./Xiaomi.js');

module.exports = function listen() {
/*
    shared.xiaomi = new Xiaomi();
    shared.xiaomi.on('info', (data) => console.log('info = ' + JSON.stringify(data)));
    shared.xiaomi.on('event', (data) => console.log('event = ' + JSON.stringify(data)));
*/
}